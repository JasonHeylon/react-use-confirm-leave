import useConfirmLeave from './';
import { renderHook, act } from '@testing-library/react-hooks';

describe('useConfirmLeave', () => {
  it('sould be ok', () => {
    expect('ok').toBe('ok');
  });
});
